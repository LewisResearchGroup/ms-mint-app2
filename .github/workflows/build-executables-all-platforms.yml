# This file is a workflow for building executables across all platforms.
# Modify the macOS build criteria accordingly.

jobs:
  # Commenting out the entire build-macos job
  # build-macos:
  #   name: Build for macOS
  #   runs-on: macos-latest
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v2
  #     - name: Install Dependencies
  #       run: |  
  #         brew install ...  
  #       
  #     - name: Build Executable
  #       run: | 
  #         ./build_macos.sh
  #     - name: Upload Artifact
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: macos-executable
  #         path: path_to_executable
  
  build-windows:
    name: Build for Windows
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Install Dependencies
        run: | 
          choco install ... 
        
      - name: Build Executable
        run: |
          ./build_windows.bat
      - name: Upload Artifact
        uses: actions/upload-artifact@v2
        with:
          name: windows-executable
          path: path_to_executable

  build-linux:
    name: Build for Linux
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Install Dependencies
        run: | 
          sudo apt-get install ... 
        
      - name: Build Executable
        run: |
          ./build_linux.sh
      - name: Upload Artifact
        uses: actions/upload-artifact@v2
        with:
          name: linux-executable
          path: path_to_executable

  release:
    name: Release
    needs: [build-windows, build-linux]  # Updating needs array to exclude build-macos
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      - name: Create macOS Archive
        run: |
          # Commenting out macOS archive creation
          # tar -czf Mint-macOS-x64.tar.gz path_to_macOS_executable

      - name: Pre-release Files
        run: |
          # Commenting out macOS release file
          # echo "Mint-macOS-x64.tar.gz" > pre_release_files.txt

      - name: Pre-release Downloads
        run: |
          # Commenting out macOS download
          # echo "Mint-macOS-x64.tar.gz" > pre_release_downloads.txt

      - name: Pre-release Installation
        run: |
          # Commenting out macOS installation instructions
          # echo "macOS instructions..."

      - name: Stable Release Files
        run: |
          # Commenting out Mint-macOS-x64 archive
          # echo "Mint-macOS-x64.tar.gz" > stable_release_files.txt

      - name: Stable Release Downloads
        run: |
          # Commenting out macOS download
          # echo "Mint-macOS-x64.tar.gz" > stable_release_downloads.txt

      - name: Stable Release Installation
        run: |
          # Commenting out macOS installation instructions
          # echo "macOS instruction..."
