name: Build Executables (Optimized)

on:
  push:
    tags:
      - "v*"   # Build on version tags
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-windows:
    name: Build Windows Executable
    runs-on: windows-latest
    timeout-minutes: 180

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Free up disk space
        run: |
          Remove-Item -Recurse -Force "C:\hostedtoolcache\windows\Python\3.7" -ErrorAction SilentlyContinue
          Remove-Item -Recurse -Force "C:\hostedtoolcache\windows\Python\3.8" -ErrorAction SilentlyContinue
          Remove-Item -Recurse -Force "C:\hostedtoolcache\windows\Python\3.9" -ErrorAction SilentlyContinue
          Remove-Item -Recurse -Force "C:\hostedtoolcache\windows\Python\3.10" -ErrorAction SilentlyContinue
          Remove-Item -Recurse -Force "C:\hostedtoolcache\windows\Python\3.11" -ErrorAction SilentlyContinue
        shell: powershell
        continue-on-error: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: |
            pyproject.toml
            requirements-dev.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip wheel setuptools
          pip install pyinstaller
          pip install "matplotlib>=3.10.8"
          pip install -e . --no-cache-dir
          echo "Switching to polars-lts-cpu for broad compatibility..."
          pip uninstall -y polars
          pip install polars-lts-cpu
        shell: bash

      - name: Create Asari environment
        run: |
          cd pyinstaller
          python create_asari_env.py
        shell: bash
        env:
          PYTHONIOENCODING: utf-8

      - name: Generate matplotlib font cache
        run: |
          cd pyinstaller
          python prebuild_matplotlib_cache.py
        shell: bash
        env:
          PYTHONIOENCODING: utf-8

      - name: Build executable with PyInstaller
        run: |
          cd pyinstaller
          pyinstaller --noconfirm --clean --log-level=INFO Mint.spec
        shell: bash
        env:
          PYTHONOPTIMIZE: 1

      - name: Verify build and check size
        run: |
          if (Test-Path "pyinstaller\dist\Mint\Mint.exe") {
            Write-Host "[OK] Executable built successfully!"
            $size = (Get-ChildItem -Path "pyinstaller\dist\Mint" -Recurse | Measure-Object -Property Length -Sum).Sum
            Write-Host "Build size: $([math]::Round($size/1GB, 2)) GB"
          } else {
            Write-Host "[ERROR] Build failed"
            exit 1
          }
        shell: powershell

      - name: Get version
        id: get_version
        run: |
          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            version=${GITHUB_REF#refs/tags/}
          else
            version="dev-$(git rev-parse --short HEAD)"
          fi
          echo "version=$version" >> $GITHUB_OUTPUT
        shell: bash

      # FIX: upload parent folder so the artifact contains Mint/...
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Mint-Windows-x64-${{ steps.get_version.outputs.version }}
          path: pyinstaller/dist/
          retention-days: 90
          compression-level: 9

  build-linux:
    name: Build Linux Executable (Ubuntu 22.04)
    runs-on: ubuntu-22.04
    timeout-minutes: 180

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Free up disk space
        run: |
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force
          df -h
        continue-on-error: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: |
            pyproject.toml
            requirements-dev.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip wheel setuptools
          pip install pyinstaller
          pip install "matplotlib>=3.10.8"
          pip install -e . --no-cache-dir
          echo "Switching to polars-lts-cpu for broad compatibility..."
          pip uninstall -y polars
          pip install polars-lts-cpu

      - name: Create Asari environment
        run: |
          cd pyinstaller
          python create_asari_env.py
        continue-on-error: true
        env:
          PYTHONIOENCODING: utf-8

      - name: Generate matplotlib font cache
        run: |
          cd pyinstaller
          python prebuild_matplotlib_cache.py
        continue-on-error: true
        env:
          PYTHONIOENCODING: utf-8

      - name: Check disk space before build
        run: df -h

      - name: Build executable with PyInstaller
        run: |
          cd pyinstaller
          pyinstaller --noconfirm --clean --log-level=DEBUG Mint.spec 2>&1 | tee build.log
        env:
          PYTHONOPTIMIZE: 1

      - name: Verify build and check size
        run: |
          if [ -f "pyinstaller/dist/Mint/Mint" ]; then
            echo "[OK] Executable built successfully!"
            chmod +x pyinstaller/dist/Mint/Mint
            size=$(du -sh pyinstaller/dist/Mint | cut -f1)
            echo "Build size: $size"
          else
            echo "[ERROR] Build failed - executable not found"
            ls -la pyinstaller/dist/ || echo "dist directory does not exist"
            exit 1
          fi

      - name: Get version
        id: get_version
        run: |
          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            version=${GITHUB_REF#refs/tags/}
          else
            version="dev-$(git rev-parse --short HEAD)"
          fi
          echo "version=$version" >> $GITHUB_OUTPUT
        shell: bash

      # FIX: upload parent folder so the artifact contains Mint/...
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Mint-Linux-x64-${{ steps.get_version.outputs.version }}
          path: pyinstaller/dist/
          retention-days: 90
          compression-level: 6

  release:
    name: Create Release
    needs: [build-windows, build-linux]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')

    # FIX: required to create the release / upload assets
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List downloaded files
        run: |
          echo "Downloaded artifacts:"
          ls -lhR artifacts/

      - name: Create release archives
        run: |
          cd artifacts
          echo "Contents of artifacts directory:"
          ls -la

          # Windows - create zip including Mint/ folder
          WINDOWS_DIR=$(find . -type d -name "Mint-Windows-x64-*" | head -n 1)
          if [ -d "$WINDOWS_DIR" ]; then
            echo "Creating Windows zip from: $WINDOWS_DIR"
            (cd "$WINDOWS_DIR" && ls -la && zip -9 -r ../../Mint-Windows-x64.zip Mint/)
          else
            echo "ERROR: Windows artifact directory not found"
            exit 1
          fi

          # Linux - create tar.gz including Mint/ folder
          LINUX_DIR=$(find . -type d -name "Mint-Linux-x64-*" | head -n 1)
          if [ -d "$LINUX_DIR" ]; then
            echo "Creating Linux tar.gz from: $LINUX_DIR"
            tar -czf Mint-Linux-x64.tar.gz -C "$LINUX_DIR" Mint/
          else
            echo "ERROR: Linux artifact directory not found"
            exit 1
          fi

          echo "Final archives created:"
          ls -lh *.zip *.tar.gz

      - name: Get version info
        id: version_info
        run: |
          version=${GITHUB_REF#refs/tags/}
          if [[ "$version" =~ (alpha|beta|rc|preview) ]]; then
            is_prerelease=true
          else
            is_prerelease=false
          fi
          echo "version=$version" >> $GITHUB_OUTPUT
          echo "is_prerelease=$is_prerelease" >> $GITHUB_OUTPUT

      - name: Create Pre-release
        if: steps.version_info.outputs.is_prerelease == 'true'
        uses: softprops/action-gh-release@v2
        with:
          files: |
            Mint-Windows-x64.zip
            Mint-Linux-x64.tar.gz
          draft: false
          prerelease: true
          generate_release_notes: true
          body: |
            ## ⚠️ Pre-release / Beta Version

            ### Downloads
            - **Windows**: `Mint-Windows-x64.zip` - Extract and run `Mint/Mint.exe`
            - **Linux**: `Mint-Linux-x64.tar.gz` - Extract and run `Mint/Mint` (Ubuntu 22.04+)

            ### Windows
            ```powershell
            # Extract Mint-Windows-x64.zip
            cd Mint
            .\Mint.exe
            ```

            ### Linux
            ```bash
            tar -xzf Mint-Linux-x64.tar.gz
            cd Mint
            ./Mint
            ```
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create Stable Release
        if: steps.version_info.outputs.is_prerelease == 'false'
        uses: softprops/action-gh-release@v2
        with:
          files: |
            Mint-Windows-x64.zip
            Mint-Linux-x64.tar.gz
          draft: false
          prerelease: false
          generate_release_notes: true
          body: |
            ## 🎉 Stable Release

            ### Downloads
            - **Windows**: `Mint-Windows-x64.zip` - Extract and run `Mint/Mint.exe`
            - **Linux**: `Mint-Linux-x64.tar.gz` - Extract and run `Mint/Mint` (Ubuntu 22.04+)

            ### Windows
            ```powershell
            # Extract Mint-Windows-x64.zip
            cd Mint
            .\Mint.exe
            ```

            ### Linux
            ```bash
            tar -xzf Mint-Linux-x64.tar.gz
            cd Mint
            ./Mint
            ```
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
